{% extends 'base.html.twig' %}

{% block title %}Location{% endblock %}

{% block body %}
    <h1>Location</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ location.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ location.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_location_index') }}">back to list</a>

    <a href="{{ path('app_admin_location_edit', {'id': location.id}) }}">edit</a>

    {{ include('admin_location/_delete_form.html.twig') }}
{% endblock %}
